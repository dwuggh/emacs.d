;; -*- lexical-binding: t; -*-


(use-package evil
  :config
  (evil-mode 1)
  )

(use-package evil-snipe
  )

;; temp location
(use-package which-key
  :init
  (which-key-mode)
  )







(provide 'init-evil)
